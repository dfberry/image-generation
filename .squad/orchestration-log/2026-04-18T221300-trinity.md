# Orchestration: Trinity — CONTRIBUTING.md CLI Fixes

**Agent:** Trinity (Backend Dev)  
**Date:** 2026-04-18T22:13:00Z  
**Task:** Fix CONTRIBUTING.md CLI flags and dev setup  
**Mode:** background  

## Fixes Applied

1. **CLI flags corrected** ✅
   - `--refiner` → `--refine` (matches generate.py actual parameter)
   - `--device` → `--cpu` (matches actual CPU selection pattern)
   - Added missing flags: `--steps`, `--guidance`, `--seed`, `--width`, `--height`

2. **Dev setup updated** ✅
   - Changed `pip install ruff` to `pip install -r requirements-dev.txt` for consistency with CI workflow
   - Ensures local dev environment matches CI environment

## Verification

- generate.py CLI signature verified
- CONTRIBUTING.md examples now match actual CLI behavior
- requirements-dev.txt integration consistent across dev/CI

## Outcome

Both fixes applied correctly. CONTRIBUTING.md now accurate and actionable for new contributors.
