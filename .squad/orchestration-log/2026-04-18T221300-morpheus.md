# Orchestration: Morpheus — PR #15 Blocker Fixes

**Agent:** Morpheus (Lead)  
**Date:** 2026-04-18T22:13:00Z  
**Task:** Fix 6 PR #15 blockers flagged by Neo, revise Joel Test score to 9/12  
**Mode:** background  

## Fixes Applied

1. **Makefile CRLF → LF** ✅
   - Converted Makefile to LF line endings via PowerShell byte-level write
   - Added `Makefile text eol=lf` to `.gitattributes` for persistent enforcement

2. **batch_observability_blog.json removed** ✅
   - `git rm` removed hardcoded-path file outside Joel Test scope

3. **CI shell quoting** ✅
   - `.github/workflows/tests.yml` line 26: `pip install ruff>=0.4.0` → `pip install 'ruff>=0.4.0'`

4. **CI uses requirements-dev.txt** ✅
   - Test job now `pip install -r requirements-dev.txt` vs manual package list
   - Torch CPU install kept separate for special index URL

5. **ruff.toml clarified** ✅
   - `line-length = 120` at top level (controls formatter), inline comment explaining E501 ignore at rule level
   - Resolved contradiction between format and lint scopes

6. **Joel Test Score Revision** ✅
   - Honest reassessment: 9/12 (source control, build, CI, bug DB, fix-first, spec, tools, testers pass; schedule and user testing fail)
   - PR description updated to reflect 9/12

## Verification

- Makefile: LF confirmed, ruff passes clean
- CI: shell quoting prevents bash redirect
- requirements-dev.txt integrated into test job
- ruff.toml intent clarified with comment

## Outcome

All 6 fixes completed. Branch ready for merge. PR #15 re-squashed to 1 commit (6c10f02), force-pushed with updated title/description reflecting 9/12 score.
