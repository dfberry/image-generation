[pytest]
markers =
    integration: integration tests that run simple_config.py as a subprocess
    performance: performance baseline tests (timing assertions)
    reproducibility: determinism tests (same inputs → identical outputs)
    visual: visual LoRA effect tests (requires generated images)
